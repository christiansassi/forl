"""User interface.

Holds the two surfaces the user sees: the notification area icon with its menu,
and the panel that opens when it is clicked, together with the drawing, motion
and desktop integration they are built from, and the application object that
keeps both in step with the poller.
"""

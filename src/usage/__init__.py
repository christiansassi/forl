"""Usage readings.

Holds the value objects every provider produces, the scheduling that takes a
reading every minute, and the two things both providers need: one HTTP call
shape and one set of expected failures.
"""
